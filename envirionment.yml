name: d100_env
channels:
  - conda-forge
  - nodefaults
dependencies:
  # defaults
  - python=3.10
  - pip
  - pre-commit
  - pytest>=6
  - pytest-cov
  - setuptools-scm
  # data
  - pandas
  - numpy
  - dask-ml
  - ydata-profiling
  - polars #追加できているかきちんと確認
  # interactive
  - ipykernel
  # plots
  - matplotlib
  # ml
  - glum
  - lightgbm
  - scikit-learn>=1.2
  - pip:
      - kagglehub #追加できているかきちんと確認
